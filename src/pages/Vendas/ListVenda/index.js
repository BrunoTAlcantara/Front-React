import { Template } from '../../../templates';

import {ListSales} from '../../../components/ListSales'

export const ListVenda= () => {
  return (
    <Template>
      <ListSales/>
    </Template>
  )
};

