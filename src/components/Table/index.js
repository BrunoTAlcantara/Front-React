import './index.css';
import {  Table } from 'antd';




export const Tabela = ({dataSource,columns}) => {
  return (
    
    <Table style={{ }} dataSource={dataSource} columns={columns}/>
    
  );
};


